import {
    NavigationMenu,
    NavigationMenuItem,
    NavigationMenuList,
} from "../ui/navigation-menu";
import { Link } from "react-router-dom";

function Nav() {
    return (
        <nav>
            <p className="text-red-800">teste</p>
        </nav>
    );
}

export default Nav;
